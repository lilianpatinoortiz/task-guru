{{#if logged_in}}
  <div class="row mb-4 welcome">
    <div class="col-md-12">
      <h6> Welcome {{name}}</h6>
    </div>
  </div>
{{else}}
  <div class="row mb-4 welcome">
    <div class="col-md-12">
      <h6> Waiting for guru...</h6>
    </div>
  </div>
{{/if}}